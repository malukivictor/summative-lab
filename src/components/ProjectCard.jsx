const ProjectCard = ({ project }) => {
  return (
    <div>
      <div>
    
      </div>
      <h3>{project.title}</h3>
      <p>{project.description}</p>
    </div>
  )
}

export default ProjectCard